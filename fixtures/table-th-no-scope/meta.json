{
  "id": "table-th-no-scope",
  "description": "A <th> with no scope attribute at all — falls back to a plain cell rather than columnheader/rowheader, since the converter only infers header direction from an explicit scope=\"col\"/\"row\" and does not implement HTML-AAM's implicit header-association algorithm. This is extremely common real-world markup (most authors omit scope), captured here as a baseline, not an endorsement of the current fallback",
  "role": "table",
  "rolesCovered": [
    "table",
    "row",
    "cell"
  ],
  "sourceRef": "https://github.com/readium/guided-navigation/tree/main/examples/read-aloud",
  "inputKind": "fragment"
}
