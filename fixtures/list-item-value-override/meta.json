{
  "id": "list-item-value-override",
  "description": "An <ol> whose second <li value=\"10\"> resets the running count, so the third item is implicitly 11 — the value attribute is not read anywhere in the converter. A correct position-numbering implementation has to track this running override per HTML's own <li value> semantics (a value change persists to every following sibling until overridden again), not just enumerate DOM child index",
  "role": "list",
  "rolesCovered": [
    "list",
    "listItem"
  ],
  "sourceRef": "https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/li#value",
  "inputKind": "fragment"
}
